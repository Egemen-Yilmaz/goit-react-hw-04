# Example env file for the Unsplash API. DO NOT commit real secrets to source control.
# Copy this file to `.env` and fill the value (the repo `.gitignore` already ignores `.env`).

# Vite will expose env variables that start with VITE_ to the client code.
VITE_UNSPLASH_ACCESS_KEY="<YOUR_UNSPLASH_ACCESS_KEY_HERE>"

# On Vercel, set the same variable key in the Project Settings -> Environment Variables.